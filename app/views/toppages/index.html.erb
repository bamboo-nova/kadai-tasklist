<% if logged_in? %>
  <% redirect_to root_url %>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Let's manage your Tasklists</h1>
      <%= link_to 'Sign up now!', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>
